export default class RowModel {
  constructor(
    public id: number,
    public node: string,
    public a: string,
    public b: string,
    public nul: string,
    public isInitial: boolean,
    public isFinal: boolean
  ) {}
}
