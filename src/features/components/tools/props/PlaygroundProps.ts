import {
  DraggableStateModel,
  TransitionModel,
  RowModel,
} from "../../../../models";

export type ToolsPlaygroundProps = {
  states: DraggableStateModel[];
  transitions: TransitionModel[];
};
